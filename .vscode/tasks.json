{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "watch",
            "command": "npm.cmd",
            "args": ["run", "watch"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": "$tsc-watch",
            "isBackground": true,
            "presentation": {
                "reveal": "never"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
